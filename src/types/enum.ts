export enum PagesNamespace {
  INDEX = "/",
  ABOUT = "/about",
  TRIP = "/trip",
  DOCS = "/docs",

  COUNTERPARTY = "/counterparty",
  DRIVER = "/driver",
  CAR = "/car",
}

export enum EQuantityUnit {
  PIECES = "шт",
  HOURS = "ч",
}
